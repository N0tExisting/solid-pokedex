/* eslint-disable @typescript-eslint/no-non-null-assertion */
import 'windi.css';

const el = document.getElementById('root')!;

el.innerHTML = `
<div>
	<h1>Sorry, this browser doesn't support es-modules!</h1>
</div>
`;

export {};
